# Actions server dependencies
# Uses rasa-sdk (NOT the full rasa package) + backend modules it imports

rasa-sdk==3.6.2

# HTTP requests (actions.py calls backend API)
requests==2.32.5
urllib3==2.5.0

# Environment
python-dotenv==1.2.1

# Backend planner/food modules need these at import time
python-dateutil==2.8.2
